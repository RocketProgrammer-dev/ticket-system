{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=ticketdb;Username=postgres;Password=CHANGE_ME"
  },
  "Jwt": {
    "_comment": "Must be at least 32 characters or HMAC-SHA256 will refuse to sign. Generate a real random one and keep it out of Git for the final version - use dotnet user-secrets.",
    "Key": "CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
